# Airtable → Shopify Draft Creator

Version 0.5.0 photo fix.

Creates Shopify draft first, then uploads each `Tech Photos` attachment to the product image endpoint.

Test with:

```env
DRY_RUN=true
LOG_LEVEL=debug
```
